name: pipe
on: [push]

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipe:
    uses: openmfp/gha/.github/workflows/pipeline-node-app.yml@make-it-possible-to-overwrite-the-chart-name
    secrets: inherit
    with:
      imageTagName: ghcr.io/openmfp/apeiro-portal
      componentVersionKey: apeiro-portal
