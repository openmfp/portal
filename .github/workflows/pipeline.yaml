name: pipe
on: [push]

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  pipe:
    uses: openmfp/gha/.github/workflows/pipeline-node-app.yml@main
    secrets: inherit
    with:
      imageTagName: ghcr.io/openmfp/apeiro-portal
      componentVersionKey: apeiro-portal
      chart: apeiro-portal
      release_branch: apeirora
      versionPrefix: apeiro-
